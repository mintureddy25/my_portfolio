import { ExperienceLayout } from '@/components/ExperienceLayout';

export const experience = {
  title: 'Cognizant Technology Solutions',
  description: `At Cognizant Technology Solutions in Chennai, I worked as a Software Engineer and CSD Intern. As a Software Engineer, I led the development of an employee management website for Walgreens, including the successful creation of a leave portal, implementation of an RTO tracking feature, and improving user authentication workflows. During my internship, I gained hands-on experience in full-stack development using AngularJS, HTML, CSS, and Java Spring Boot.`,
  date: 'Sept 2022 – Apr 2024', // Updated date field
  details: [
    {
      role: 'Software Engineer',
      company: 'Cognizant Technology Solutions',
      location: 'Chennai',
      dateRange: 'May 2023 – Apr 2024',
      responsibilities: [
        'Led the development of an employee management website for Walgreens, including the successful end-to-end creation of a leave portal and the integration of an RTO tracking feature.',
        'Implemented user authentication and authorization workflows using React and Node.js with JWT tokens, optimizing secure access.',
        'Developed and optimized features for the employee portal, integrating third-party APIs to fetch employee data for enhanced reporting.',
        'Built and managed role-based access control (RBAC) for the portal, ensuring that employees had tailored access to features based on roles.',
        'Enhanced performance by optimizing dynamic content rendering using Next.js, ensuring fast load times and efficient client-side and server-side rendering.',
        'Implemented an advanced notification system using Node.js and React, optimizing the real-time delivery of notifications with SSR in Next.js.',
      ],
    },
    {
      role: 'CSD Intern',
      company: 'Cognizant Technology Solutions',
      location: 'Chennai',
      dateRange: 'Sept 2022 – Mar 2023',
      responsibilities: [
        'Gained hands-on experience in full-stack development using AngularJS, HTML, CSS, and Java Spring Boot.',
        'Assisted in the development and optimization of web applications, gaining practical experience with both front-end and back-end technologies.',
      ],
    },
  ],
};

export const metadata = {
  title: experience.title,
  description: experience.description,
};

export default function ExperiencePage(props) {
  return (
    <ExperienceLayout experience={experience} {...props}>
      <section>
        <h2>Experience Details</h2>
        <p>
          <strong>Date:</strong> {experience.date}
        </p>
        <p>{experience.description}</p>
        {experience.details.map((role, index) => (
          <div key={index}>
            <h3>{role.role}</h3>
            <p>
              <strong>Company:</strong> {role.company}
            </p>
            <p>
              <strong>Location:</strong> {role.location}
            </p>
            <p>
              <strong>Date Range:</strong> {role.dateRange}
            </p>
            <h4>Responsibilities</h4>
            <ul>
              {role.responsibilities.map((responsibility, idx) => (
                <li key={idx}>{responsibility}</li>
              ))}
            </ul>
          </div>
        ))}
      </section>
    </ExperienceLayout>
  );
}
